apiVersion: v1
kind: Service
metadata:
  name: go-api-service
  labels:
    app: go-api
spec:
  type: NodePort
  selector:
    app: go-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
