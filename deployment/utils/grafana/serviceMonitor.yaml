apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: go-api-monitor
  labels:
    release: prometheus   # MUST match your Prometheus Helm release name
spec:
  selector:
    matchLabels:
      app: go-api
  endpoints:
    - port: http
      path: /metrics
      interval: 5s